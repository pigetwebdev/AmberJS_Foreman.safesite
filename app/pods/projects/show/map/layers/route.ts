import Route from '@ember/routing/route'

export default class ProjectsShowMapLayers extends Route.extend({
    // anything which *must* be merged to prototype here
}) {
    // model() {
    //     return this.modelFor('projects.show')
    // }
}
